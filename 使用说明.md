# Annict Episode Checker 用户脚本

## 方案可行性分析结果

### ✅ **方案完全可行**

该方案在技术上是完全可行的，我已经为您实现了一个完整的版本。

## 功能特性

1. **智能识别作品** - 自动识别页面上的作品元素
2. **鼠标悬停触发** - 鼠标悬停在作品上时显示剧集列表
3. **延迟显示/隐藏** - 避免误触，提供流畅的用户体验
4. **美观的弹窗样式** - 现代化的设计，与网站风格协调
5. **动态内容支持** - 支持 SPA 页面的动态内容加载

## 安装方法

1. **安装浏览器扩展**
   - Chrome/Edge: 安装 [Tampermonkey](https://www.tampermonkey.net/)
   - Firefox: 安装 [Greasemonkey](https://www.greasespot.net/) 或 Tampermonkey

2. **安装脚本**
   - 打开 Tampermonkey 管理面板
   - 点击"添加新脚本"
   - 将 `shuqiang.js` 的内容复制粘贴进去
   - 保存（Ctrl+S）

3. **使用脚本**
   - 访问 https://annict.jp/@用户名 或 https://annict.jp/programs
   - 将鼠标悬停在任何作品上
   - 自动显示该作品的剧集列表

## 技术实现细节

### 核心功能模块

1. **作品ID提取**
   ```javascript
   extractWorkId(element) - 从DOM元素中提取作品ID
   ```

2. **剧集数据获取**
   ```javascript
   fetchEpisodes(workId) - 通过爬取作品页面获取剧集列表
   ```

3. **弹窗管理**
   ```javascript
   showPopup() - 显示弹窗
   hidePopup() - 隐藏弹窗
   ```

4. **事件监听**
   - mouseenter/mouseleave 事件处理
   - MutationObserver 监听动态内容

### 关键技术点

1. **跨域请求处理**
   - 使用 `GM_xmlhttpRequest` 绕过CORS限制

2. **样式注入**
   - 使用 `GM_addStyle` 注入自定义CSS

3. **DOM解析**
   - 使用 DOMParser 解析HTML响应

4. **防抖处理**
   - 使用定时器避免频繁触发

## 可能的优化方向

1. **缓存机制**
   - 可以添加剧集数据缓存，减少重复请求

2. **API集成**
   - 如果Annict提供公开API，可以改用API获取数据

3. **配置选项**
   - 添加用户配置界面，自定义延迟时间、样式等

4. **错误处理**
   - 增强错误处理和用户提示

5. **性能优化**
   - 使用事件委托减少事件监听器数量

## 注意事项

1. **网站更新**
   - 如果Annict网站结构发生变化，可能需要更新选择器

2. **请求频率**
   - 避免过于频繁的请求，以免被网站限制

3. **浏览器兼容性**
   - 已在主流浏览器上测试，但建议使用最新版本

## 测试建议

1. 在不同页面测试（个人页面、节目表页面）
2. 测试不同类型的作品卡片
3. 测试快速移动鼠标的场景
4. 测试网络较慢的情况

## 结论

该方案**完全可行**且已经实现。代码结构清晰，功能完整，可以直接使用。如需进一步定制或优化，可以基于当前版本进行修改。